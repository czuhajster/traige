services:
  demo-producer:
    build:
      context: ./demo-producer
    restart: "on-failure:5"
    ports:
      - "80:8080"
  backend:
    build:
      context: ./backend
    restart: "on-failure:5"
